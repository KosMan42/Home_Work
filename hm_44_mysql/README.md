# Домашнее задание: MySQL (Percona) Репликация + GTID

## Окружение

Работа выполнена на виртуальных машинах:

- Vagrant 2.4.9
- VirtualBox 7.2.6a
- Базовый образ: ubuntu/jammy64

Развертывание выполнено с использованием **Vagrant provisioning (shell-скрипты)**.

---

## Цель работы

В рамках задания реализованы:

1. Установка Percona Server
2. Настройка GTID репликации
3. Настройка master → slave
4. Исключение таблиц из репликации

---

## Развертывание

Стенд разворачивается автоматически через `Vagrantfile`.

Используются provisioning-скрипты:

* `provision/master.sh`
* `provision/slave.sh`

Скрипты выполняют:

* установку Percona Server
* настройку конфигурации MySQL
* создание базы и загрузку дампа
* создание пользователя репликации
* настройку и запуск репликации

## 1. Настройка Master

На master:

* включён бинарный лог
* установлен `server-id = 1`
* включён GTID

Создана база `bet` и загружен тестовый дамп.

Создан пользователь репликации:

---

## 2. Настройка Slave

На slave:

* установлен `server-id = 2`
* включён GTID

Загружен дамп master.

Настроено подключение.

Запущенп репликация.

---

## 3. Исключение таблиц

В конфигурации slave:

```ini
replicate-ignore-table = bet.events_on_demand
replicate-ignore-table = bet.v_same_event
```
---

## Проверка работы

### Статус репликации

```bash
sudo mysql -e "SHOW REPLICA STATUS\G"
```

Результат приведен в файле replica_status.txt

---

### Итог

* Развертывание выполнено автоматически через Vagrant
* Использованы provisioning-скрипты
* Настроена GTID репликация
* Реализовано исключение таблиц
* Репликация успешно проверена
